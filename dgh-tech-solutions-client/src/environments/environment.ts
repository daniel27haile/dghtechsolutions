// Development environment
// Used automatically by `ng serve` and `ng build` (without --configuration production)
export const environment = {
  production: false,
  apiUrl: 'http://localhost:8000/api',
};
